{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "drop-window",
  "description": "Capability for the main window",
  "windows": ["drop-window"],
  "permissions": [
    "core:default",
    "core:window:allow-hide"
  ]
}
